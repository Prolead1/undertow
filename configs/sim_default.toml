# Default config for undertow.sim — skeleton. Filled by S01.
# See CONTRACTS.md §2 for the full SimConfig tree.

[episode]
# duration_days, step_minutes, agent_capital_usdc, marginal_agent, fee_tier_bps, tick_spacing

[action_grid]
# center_offsets, widths, include_hold

[gas]
# mint_units, burn_units, collect_units, rebalance_swap_units

[slippage]
# fixed_impact_bps

[reward]
# risk_penalty_lambda, risk_rolling_window_steps, normalize_by_capital,
# ablation flags: fees_enabled, gas_enabled, slippage_enabled, il_enabled, risk_penalty_enabled

[split]
# train_start_utc, train_end_utc, eval_start_utc, eval_end_utc

[training]
# algorithm, seeds, discount_gamma, gae_lambda, clip_epsilon,
# hidden_size, n_hidden, parallel_envs, total_timesteps,
# checkpoint_freq_steps, log_freq_steps

[backtest]
# compute_decomposition, log_decision_every_step

[price]
# mode: "replay" | "calibrated"